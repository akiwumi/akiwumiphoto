import Link from 'next/link';
import styles from './Outreach.module.css';
import { ADDRESS_BOOK, ADDRESS_BOOK_SOURCE } from '@/lib/outreach/address-book';
export default function OutreachPage() { const countries = new Set(ADDRESS_BOOK.map((contact) => contact.country)).size; return <div className={styles.page}><header className={styles.header}><div><p className={styles.eyebrow}>Private outreach desk · local address book</p><h1 className={styles.title}>Interior designer outreach</h1><p className={styles.subtitle}>A considered place to review contacts, prepare personal invitations, and keep every conversation accountable.</p></div><div className={styles.actions}><Link className={styles.buttonSecondary} href="/admin/outreach/import">Review source sheet</Link><Link className={styles.button} href="/admin/outreach/campaigns/new">New campaign</Link></div></header><section className={styles.grid}>{[['Contacts',String(ADDRESS_BOOK.length),`${countries} countries`],['Approved audience','0','Review contacts before sending'],['In flight','0','No messages sent'],['Replies','0','No replies recorded']].map(([label,value,meta])=><article className={styles.stat} key={label}><div className={styles.statLabel}>{label}</div><div className={styles.statValue}>{value}</div><div className={styles.statMeta}>{meta}</div></article>)}</section><section className={styles.panel}><div className={styles.panelHead}><h2 className={styles.panelTitle}>Imported address book</h2><span className={styles.chip}>{ADDRESS_BOOK_SOURCE}</span></div><table className={styles.table}><thead><tr><th>Contact</th><th>Country</th><th>Primary email</th><th>Approval</th></tr></thead><tbody>{ADDRESS_BOOK.slice(0, 6).map((contact)=><tr key={contact.id}><td><strong>{contact.name}</strong><br/><span>{contact.studio} · {contact.role}</span></td><td>{contact.country}</td><td>{contact.email}</td><td><span className={`${styles.chip} ${styles.chipWarn}`}>needs review</span></td></tr>)}</tbody></table></section></div>; }
