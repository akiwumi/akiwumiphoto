import Link from 'next/link'; import styles from '../Outreach.module.css'; import ImportClient from './ImportClient';
import { getAddressBook } from '@/lib/outreach/address-book-server';
import { getOutreachContactCategories } from '@/lib/outreach/categories-server';
export default async function ImportPage(){ const [contacts, categories] = await Promise.all([getAddressBook(), getOutreachContactCategories()]); return <div className={styles.page}><header className={styles.header}><div><p className={styles.eyebrow}>Step 01 · intake</p><h1 className={styles.title}>Import contacts</h1><p className={styles.subtitle}>Upload the workbook and import valid rows directly into Supabase and the address book.</p></div><Link className={styles.buttonSecondary} href="/admin/outreach/contacts">Back to contacts</Link></header><ImportClient initialContacts={contacts} initialCategories={categories} /></div>}
