'use client';
import { useState } from 'react'; import styles from '../Outreach.module.css';
export default function ImportClient(){ const [file,setFile]=useState(''); const [confirmed,setConfirmed]=useState(false); return <section className={styles.panel}><div className={styles.notice}>Local mock mode is active. The workbook is previewed in the browser; importing requires an explicit confirmation and never sends email.</div><div className={styles.formGrid}><div className={`${styles.field} ${styles.fieldFull}`}><label htmlFor="workbook">.xlsx workbook</label><input className={styles.input} id="workbook" type="file" accept=".xlsx" onChange={(event)=>setFile(event.target.files?.[0]?.name ?? '')}/>{file&&<small>{file} · ready for mapping</small>}</div><div className={styles.field}><label>Email column</label><select className={styles.select} defaultValue="Email"><option>Email</option><option>Contact email</option></select></div><div className={styles.field}><label>First name column</label><select className={styles.select} defaultValue="First name"><option>First name</option><option>None</option></select></div><div className={`${styles.field} ${styles.fieldFull}`}><label>Preview</label><div className={styles.panel}><table className={styles.table}><thead><tr><th>Email</th><th>First name</th><th>Company</th><th>Result</th></tr></thead><tbody><tr><td>anna@ateliernord.se</td><td>Anna</td><td>Atelier Nord</td><td><span className={`${styles.chip} ${styles.chipGood}`}>valid</span></td></tr><tr><td>hello@studioberg.dk</td><td>Mikael</td><td>Studio Berg</td><td><span className={`${styles.chip} ${styles.chipWarn}`}>needs review</span></td></tr></tbody></table></div></div><div className={`${styles.field} ${styles.fieldFull}`}><button type="button" className={styles.button} onClick={()=>setConfirmed(true)} disabled={!file}>{confirmed?'Import confirmed · 2 rows queued for review':'Confirm import'}</button></div></div></section> }

