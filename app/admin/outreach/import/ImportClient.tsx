'use client';
import { useState } from 'react'; import styles from '../Outreach.module.css'; import { ADDRESS_BOOK, ADDRESS_BOOK_SOURCE } from '@/lib/outreach/address-book';
export default function ImportClient(){ const [file,setFile]=useState(''); const [confirmed,setConfirmed]=useState(false); return <section className={styles.panel}><div className={styles.notice}>Source loaded locally from {ADDRESS_BOOK_SOURCE}. These contacts remain unapproved until you review them. No messages are sent.</div><div className={styles.formGrid}><div className={`${styles.field} ${styles.fieldFull}`}><label htmlFor="workbook">Source workbook</label><input className={styles.input} id="workbook" type="file" accept=".xlsx" onChange={(event)=>setFile(event.target.files?.[0]?.name ?? '')}/>{file&&<small>{file} · local preview only</small>}</div><div className={`${styles.field} ${styles.fieldFull}`}><label>Address book preview · {ADDRESS_BOOK.length} rows</label><div className={styles.panel}><table className={styles.table}><thead><tr><th>Name</th><th>Studio</th><th>Primary email</th><th>Country</th></tr></thead><tbody>{ADDRESS_BOOK.slice(0, 5).map((contact)=><tr key={contact.id}><td>{contact.name}</td><td>{contact.studio}</td><td>{contact.email}</td><td>{contact.country}</td></tr>)}</tbody></table></div></div><div className={`${styles.field} ${styles.fieldFull}`}><button type="button" className={styles.button} onClick={()=>setConfirmed(true)}>{confirmed?'Address book confirmed · contacts remain for review':'Confirm address book'}</button></div></div></section> }
